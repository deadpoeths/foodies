import 'package:flutter/material.dart';

class CustomerOrderListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Order List Page'));
  }
}
